# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Unix socket connections use trust for local container access
local all all trust
# All TCP/IP connections require password authentication (IPv4 and IPv6)
host all all 0.0.0.0/0 scram-sha-256
host all all ::0/0 scram-sha-256
