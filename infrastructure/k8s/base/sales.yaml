apiVersion: apps/v1
kind: Deployment
metadata:
  name: sales
  namespace: erp-local
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sales
  template:
    metadata:
      labels:
        app: sales
    spec:
      initContainers:
      - name: wait-for-postgres
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z postgres-service 5432; do echo "Waiting for postgres..."; sleep 3; done']
      - name: wait-for-kafka
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z kafka-service 9092; do echo "Waiting for kafka..."; sleep 3; done']
      containers:
      - name: sales
        image: erp/sales
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://tempo-service:4317"
        - name: OTEL_SERVICE_NAME
          value: "sales"
        - name: PostgreSQL__ConnectionString
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: connection-string-sales
        - name: Kafka__BootstrapServers
          value: "kafka-service:9092"
        - name: Jwt__Secret
          value: "your-secret-key-change-in-production-min-32-chars"
        - name: Jwt__Issuer
          value: "erp-system"
        - name: Jwt__Audience
          value: "erp-client"
        livenessProbe:
          httpGet:
            path: /alive
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: sales-service
  namespace: erp-local
spec:
  selector:
    app: sales
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
