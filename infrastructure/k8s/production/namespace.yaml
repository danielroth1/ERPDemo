apiVersion: v1
kind: Namespace
metadata:
  name: erp-prod
  labels:
    name: erp-prod
    environment: production
    app: erp-system
  annotations:
    description: "Production environment for ERP system"
