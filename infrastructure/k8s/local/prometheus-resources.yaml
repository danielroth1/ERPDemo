apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: erp-local
spec:
  template:
    spec:
      containers:
        - name: prometheus
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
