apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml  # Local environment namespace
  - ../base         # Reference the entire base directory

# Note: namespace field not set here because base files already have hardcoded namespaces
# TODO: For proper Kustomize usage, remove 'namespace:' fields from base/*.yaml files
# and uncomment the line below to let Kustomize inject the namespace automatically:
# namespace: erp-local

